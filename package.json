{
  "name": "voz-reader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs && cp -r public/styles dist/styles",
    "dev": "wrangler pages dev dist --compatibility-flag=nodejs_compat",
    "lint": "eslint ."
  },
  "dependencies": {
    "hono": "^4.7.7",
    "node-html-parser": "^7.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260307.1",
    "esbuild": "^0.25.0",
    "wrangler": "^4.71.0"
  }
}
